class test {
    constructor(object) {
        this.userId = object.userId;
        this.id = object.id;
        this.title = object.title;
        this.completed = object.completed;
    }
}

module.exports = test;